node_modules/
.next/
dist/
build/
*.tsbuildinfo

.env
.env.local
.env.*.local

__pycache__/
*.py[cod]
.venv/
venv/

.DS_Store
*.log

# Large 3D assets. There is no tracked sample — this used to negate
# `models/sample-building.glb`, which has never existed. See models/README.md:
# nothing in the render path reads a model file, by decision §27.
models/*.glb
models/*.gltf

# The interview pack lives beside this repo, not in it: it is rehearsal
# material about talking to interviewers, and this repository is public.
# See ../DTwin-interview/. Ignored here so it cannot wander back in.
interview/

# The interview pack. Afsal chose "public repo, interview pack excluded" on
# 2026-09-20 and it was removed from history for that reason; it has been
# moved around since and landed here on 2026-09-24. It must never be pushed.
dtv/
