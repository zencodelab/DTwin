node_modules/
.next/
dist/
build/
*.tsbuildinfo

.env
.env.local
.env.*.local

__pycache__/
*.py[cod]
.venv/
venv/

.DS_Store
*.log

# Large 3D assets — keep the tracked sample only
models/*.glb
models/*.gltf
!models/sample-building.glb

# The interview pack lives beside this repo, not in it: it is rehearsal
# material about talking to interviewers, and this repository is public.
# See ../DTwin-interview/. Ignored here so it cannot wander back in.
interview/
